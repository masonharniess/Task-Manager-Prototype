function toggleDark() {
    var checkbox = document.getElementById("switch_checkbox_dark");
    checkbox.checked = !checkbox.checked;
}

function toggleData() {
    var checkbox = document.getElementById("switch_checkbox_share");
    checkbox.checked = !checkbox.checked;
}

function toggleFilter() {
    var checkbox = document.getElementById("switch_checkbox_filter");
    checkbox.checked = !checkbox.checked;
}

function toggleDisturb() {
    var checkbox = document.getElementById("switch_checkbox_disturb");
    checkbox.checked = !checkbox.checked;
}